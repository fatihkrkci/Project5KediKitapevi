@model List<ResultFeatureDto>

@{
    ViewData["Title"] = "FeatureList";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<style>
    .btn-group a {
        margin-right: 5px;
    }
</style>

<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h4 class="card-title">Öne Çıkarılan Kitaplar Listesi</h4>
        <a href="/Admin/Feature/CreateFeature" class="btn btn-primary">Öne Çıkarılacak Kitap Ekle</a>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table id="basic-datatables"
                   class="display table table-striped table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>KİTAP ADI</th>
                        <th>AÇIKLAMA</th>
                        <th>GÖRSEL</th>
                        <th>SEÇENEKLER</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.FeatureId</td>
                            <td>@item.BookName</td>
                            <td>@item.Description</td>
                            <td>
                                <img src="@item.ImageUrl" style="height:200px; width:auto" />
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a class="btn btn-warning" href="/Admin/Feature/UpdateFeature/@item.FeatureId"><i class="fas fa-pen"></i></a>
                                    <a class="btn btn-danger" href="/Admin/Feature/DeleteFeature/@item.FeatureId"><i class="fas fa-trash"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
